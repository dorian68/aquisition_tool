Place a trusted compiled `vbaProject.bin` here to enable default XLSM macro embedding.

The generator will automatically use `dashboard_generator/assets/vbaProject.bin` when
the requested output path ends with `.xlsm` and no `--vba-project` argument is provided.
